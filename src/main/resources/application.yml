server:
  port: 8080
spring:
  application:
    name: Bank
  datasource:
    password: password
    url: jdbc:postgresql://host.docker.internal:5432/ali
    username: ali
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
